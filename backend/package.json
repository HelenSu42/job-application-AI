{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "encore.dev": "^1.48.8",
    "argon2": "^0.31.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.7.4",
    "vitest": "^1.6.0"
  }
}